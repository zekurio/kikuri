{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch Kikuri",
        "type": "go",
        "request": "launch",
        "program": "${workspaceFolder}/cmd/kikuri/main.go",
        "cwd": "${workspaceFolder}",
        "args": [
          "-c",
          "${workspaceFolder}/config/config.toml"
        ]
      }
    ]
  }
  